import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF000000);
  static const Color cardBg = Color(0xFF111111);
  static const Color cardBg2 = Color(0xFF1A1A1A);
  static const Color inputBg = Color(0xFF1A1A1A);
  static const Color border = Color(0xFF2A2A2A);
  static const Color navBg = Color(0xFF111111);

  static const Color accent = Color(0xFF00FF85);
  static const Color green = Color(0xFF00FF85);
  static const Color red = Color(0xFFFF4D4F);
  static const Color orange = Color(0xFFF59E0B);
  static const Color purple = Color(0xFF7C3AED);

  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF9CA3AF);
  static const Color textMuted = Color(0xFF6B7280);

  static const Color dark = Color(0xFF000000);
  static const Color white = Color(0xFFFFFFFF);

  // Auth glow helpers
  static const Color accent12 = Color(0x1F00FF85); // 12 % — idle halo / meter bg
  static const Color accent20 = Color(0x3300FF85); // 20 % — soft glow shadow
  static const Color accent40 = Color(0x6600FF85); // 40 % — particle + ring glow
}
